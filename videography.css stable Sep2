body, html {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    background-color: black; /* Set background color to black */
}

header {
    position: fixed; /* Make the header fixed */
    top: 0;
    left: 0;
    width: 100%;
    height: 40vh;
    overflow: hidden;
    z-index: 1000; /* Ensure the header is above other content */
}

.video-mask {
    position: relative;
    width: 100%;
    height: 100%;
    clip-path: ellipse(60% 100% at 50% 100%); /* Clip the video to a half-ellipse facing down */
      transform: rotate(180deg); /* Rotate the half-circle */

}

.video-mask video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transform: rotate(180deg) scale(1.3); /* Rotate the half-circle */
}

.thumbnail-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    padding: 20px;
    margin-top: 50vh; /* Add margin to avoid overlap with fixed header */
}

.thumbnail {
    position: relative;
    width: calc(33.333% - 20px);
    margin: 10px;
    overflow: hidden;
    padding-top: 56.25%; /* 16:9 Aspect Ratio */
}

.thumbnail img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
}
