/* Colors */
:root {
  --primary-color: #333333; /* Dark gray for primary elements */
  --secondary-color: #666666; /* Medium gray for secondary elements */
  --accent-color: #cccccc; /* Light gray for accents */
  --text-color: #cccccc; /* Light text color */
  --background-color: #333333; /* Clean dark background */
  --button-color: #444444; /* Darker gray for buttons */
  --button-hover-color: #555555; /* Slightly lighter gray for button hover */
  --navbar-hover-color: #444444; /* Matches button color for navbar hover */
}

body {
  font-family: acumin-pro, sans-serif;
  font-weight: 300;
  color: var(--text-color);
  background-color: white;
}

header {
  font-family: acumin-pro, sans-serif;
  text-align: center;
  padding: 21px;
  background-color: var(--primary-color);
}

.gallery {
  display: inline-flex;
  flex-wrap: wrap;
  justify-content: center;
  padding: 100px 10px 10px; /* Add top padding to separate from navbar */
}

.individual {
  position: relative;
  display: inline-block;
  width: 200px; /* Adjust as needed */
  margin: 10px;
  text-align: center;
}

.individual img {
  width: 100%;
  height: auto;
  border-radius: 0px;
  object-fit: cover;
  border: 0px solid #ddd;
  transition: box-shadow 0.2s;
}

.individual:hover img {
  box-shadow: 0 0 10px 5px rgba(255, 255, 255, 0.8); /* Highlight effect */
}

.individual p {
  margin: 0;
  padding: 0px 0;
  font-size: 16px;
  color: white;
  background-color: black;
  width: 100%;
  position: absolute;
  bottom: 0px;
  left: 0;
  display: inline-block; /* Always visible */
}

/* Mobile styling */
@media (max-width: 768px) {
  .individual {
    width: 100%; /* Full width */
    max-width: 600px;
    margin: 0 auto 10px;
  }

  .individual img {
    width: 100%;
    height: auto;
    max-width: 600px;
    box-shadow: none; /* No shadow on mobile */
  }

  .individual p {
    font-size: 2.2em; /* Adjust text size as needed */
    text-decoration: none;
    text-transform: uppercase;
    padding: 20px 0; /* Adjust padding for better appearance */
  }
}

h1,
h2,
h3 {
  font-family: acumin-pro, sans-serif;
  font-weight: bold;
}

.logo img {
  height: 60px;
  padding-right: 20px; /* Add padding to ensure visibility */
  transition: all 0.4s ease;
}

.logo img:hover {
  filter: brightness(1);
}

.navbar {
  display: flex;
  justify-content: flex-start; /* Align items to the start */
  align-items: center;
  text-transform: uppercase;
  padding: 10px 10px;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  background-color: transparent;
  z-index: 1000;
  transition: background-color 0.3s ease;
}

.navbar.scrolled {
  background-color: rgba(51, 51, 51, 0.1); /* semi-transparent background */
  backdrop-filter: blur(10px); /* blur effect */
  padding: 10px 10px;
}

.hamburger {
  display: none;
  flex-direction: column;
  cursor: pointer;
  z-index: 1001;
  position: absolute;
  right: 20px; /* Adjust this value as needed to ensure visibility */
  top: 15px;
  transition: all 0.4s ease;
}

.hamburger span {
  height: 3px;
  width: 35px;
  background: white;
  margin: 5px;
  transition: all 0.3s;
}

.hamburger:hover span {
  background: var(--accent-color);
}

.nav-links {
  display: flex;
  justify-content: flex-start; /* Align items to the start */
  align-items: center;
  margin-left: 20px; /* Adding space between the logo and navigation bar */
}

.nav-links a {
  padding: 0 14px;
  text-decoration: none;
  color: white;
  font-size: 20px;
  transition: color 0.3s ease;
}

.nav-links a:hover {
  color: var(--accent-color);
}

@media screen and (min-width: 820px) {
  .logo {
    position: relative;
    top: 0px; /* Adjust this value to position the logo slightly below */
    left: 20px; /* Adjust this value to move the logo to the right */
  }
}

@media screen and (max-width: 819px) {
  .navbar {
    justify-content: center; /* Center items on mobile */
  }

  .hamburger {
    display: flex;
    position: absolute;
    right: 45px;
    top: 22px;
  }

  .nav-links {
    position: fixed;
    right: 0;
    top: 10px;
    height: calc(100vh - 60px);
    width: 100%;
    background-color: rgba(51, 51, 51, 0.8); /* semi-transparent background */
    backdrop-filter: blur(10px); /* blur effect */
    flex-direction: column;
    text-transform: uppercase;
    justify-content: center; /* Center the links */
    align-items: center;
    opacity: 0; /* Start hidden */
    visibility: hidden; /* Prevent clicks when hidden */
    transform: translateY(-20px); /* Slightly move the menu up */
    transition: opacity 0.5s ease, visibility 0.5s ease, transform 0.5s ease; /* Smooth transitions */
  }

  .nav-links.open {
    opacity: 1; /* Show menu */
    visibility: visible; /* Allow clicks */
    transform: translateY(0); /* Reset position */
  }

  .nav-links a {
    padding: 15px 0;
    font-size: 24px;
    transition: color 0.3s ease, transform 0.3s ease;
  }

  .nav-links a:hover {
    color: var(--accent-color);
    transform: scale(1.1);
  }

  .hamburger.open span:nth-child(1) {
    transform: rotate(-45deg) translate(-5px, 5px);
  }

  .hamburger.open span:nth-child(2) {
    opacity: 0;
  }

  .hamburger.open span:nth-child(3) {
    transform: rotate(45deg) translate(-5px, -5px);
  }
}