/* CSS Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html,
body {
  width: 100%;
  height: 100%;
  background-color: var(--background-color);
  color: var(--text-color);
}

/* Colors */
:root {
  --primary-color: #333333; /* Dark gray for primary elements */
  --secondary-color: #666666; /* Medium gray for secondary elements */
  --accent-color: #de8435; /* Orange for accents */
  --text-color: #cccccc; /* Light gray for text */
  --background-color: #000000; /* Black for background */
  --button-color: #444444; /* Dark gray for buttons */
  --button-hover-color: #555555; /* Slightly lighter gray for button hover state */
  --navbar-hover-color: #444444; /* Dark gray for navbar hover state */
}

.container {
  width: 100%;
  max-width: 2200px; /* Adjust the max-width as needed */
  margin: 0 auto;
  padding: 0 20px; /* Optional padding */
}

.masthead {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 60vh;
  background-size: cover;
  background-position: center;
  color: var(--text-color);
  text-align: center;
  transition: background-image 1s ease-in-out; /* Smooth transition */

}

.fade-in {
  opacity: 0;
  animation: fadeIn 1s forwards;
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

.masthead h1 {
  font-size: 3em;
  margin-bottom: 10px;
}

.masthead p {
  font-size: 1.2em;
}

.gallery {
  background-color: var(--background-color);
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  padding: 20px; /* Reduced padding to fit more images */
}

.individual {
  position: relative;
  display: inline-block;
  width: 100px; /* Reduced width for thumbnails */
  margin: 10px; /* Reduced margin to fit more images */
  text-align: center;
}

.individual img {
  width: 100%;
  height: auto;
  border-radius: 5px; /* Reduced border-radius for smaller images */
  object-fit: cover;
  border: 1px solid var(--background-color); /* Reduced border size */
  transition: box-shadow 0.2s, transform 0.2s;
}

.individual:hover img {
  transform: scale(1.05);
  box-shadow: 0 0 10px 5px rgba(255, 255, 255, 0.8); /* Highlight effect */
}

.individual p {
  margin: 0;
  padding: 5px 0; /* Reduced padding for smaller text area */
  font-size: 12px; /* Reduced font size */
  color: var(--text-color);
  background-color: var(--accent-color);
  width: 100%;
  position: absolute;
  bottom: 0;
  left: 0;
  border-radius: 0 0 5px 5px; /* Reduced border-radius */
}

/* Mobile styling */
@media (max-width: 800px) {
  .individual {
    width: 80px; /* Smaller width for mobile */
    margin: 5px;
  }

  .individual img {
    width: 100%;
    height: auto;
    box-shadow: none; /* No shadow on mobile */
    object-fit: contain;
  }

  .individual p {
    font-size: 10px; /* Smaller text size for mobile */
    padding: 5px 0; /* Adjust padding for better appearance */
  }
}

/* Desktop styling */
@media (min-width: 801px) {
  .individual {
    width: 100px; /* Consistent width for desktop */
    margin: 5px;
  }

  .individual img {
    width: 100%;
    height: auto;
    box-shadow: none; /* No shadow on mobile */
    object-fit: cover;
  }

  .individual p {
    font-size: 12px; /* Consistent text size for desktop */
    padding: 5px 0; /* Adjust padding for better appearance */
  }
}

.footer {
  background-color: var(--primary-color);
  color: var(--text-color);
  padding: 20px 0;
  text-align: center;
}

.footer-content {
  display: flex;
  justify-content: space-around;
  flex-wrap: wrap;
  padding: 20px;
}

.footer-section {
  flex: 1;
  margin: 10px;
}

.footer-section h3 {
  margin-bottom: 10px;
}

.footer-section p,
.footer-section ul {
  margin: 0;
  padding: 0;
  list-style: none;
}

.footer-section ul li {
  margin-bottom: 10px;
}

.footer-section ul li a {
  color: var(--text-color);
  text-decoration: none;
  transition: color 0.3s ease;
}

.footer-section ul li a:hover {
  color: var(--accent-color);
}

.social-icons {
  display: flex;
  justify-content: center;
  gap: 10px;
}

.social-icons a {
  color: var(--text-color);
  transition: color 0.3s ease;
}

.social-icons a:hover {
  color: var(--accent-color);
}

.footer-bottom {
  margin-top: 20px;
}

.back-to-top {
  color: var(--accent-color);
  text-decoration: none;
  transition: color 0.3s ease;
}

.back-to-top:hover {
  color: var(--text-color);
}
