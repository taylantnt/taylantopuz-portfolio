/* CSS Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html,
body {
  width: 100%;
  height: 100%;
  background-color: white;
  color: var(--text-color);
  font-family: 'futura-pt', sans-serif;
}

/* Scrolled State for Navbar */
.navbar.scrolled {
  background-color: rgba(51, 51, 51, 1); /* Semi-transparent background */
  padding: 10px 10px; /* Padding inside the navbar */
}

/* Colors */
:root {
  --primary-color: #333333; /* Dark gray for primary elements */
  --secondary-color: #666666; /* Medium gray for secondary elements */
  --accent-color: #de8435; /* Orange for accents */
  --text-color: #cccccc; /* Light gray for text */
  --background-color: #000000; /* Black for background */
  --button-color: #444444; /* Dark gray for buttons */
  --button-hover-color: #555555; /* Slightly lighter gray for button hover state */
  --navbar-hover-color: #444444; /* Dark gray for navbar hover state */
}

.container {
  width: 100%;
  max-width: 1200px; /* Adjust the max-width as needed */
  margin: 0 auto;
  padding: 20px; /* Optional padding */
  padding-bottom: 50px; /* Add some padding at the bottom for spacing */
}

.gallery {
  display: grid;
  gap: 5px;
}

.individual {
  position: relative;
  overflow: hidden;
  border-radius: 0px;
  transition: transform 0.3s;
}

.individual img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.3s, filter 0.3s;
  z-index: 1; /* Ensure the image is in the correct stacking context */
}

.individual::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(222, 132, 53, 0.4); /* Your accent color with some transparency */
  opacity: 0; 
  transition: opacity 0.3s;
  z-index: 2;  /* Remove or adjust mix-blend-mode if needed */
}

.individual:hover::before {
  opacity: 0.3; /* Adjust the opacity as needed */
}

.individual:hover {
  transform: scale(1.05);
}

.individual:hover img {
  transform: scale(1.1);
  filter: brightness(0.7);
}

.individual p {
  position: absolute;
  bottom: 10px;
  left: 0px;
  padding: 5px 10px;
  font-size: 14px;
  color: black;
  background-color: rgba(255, 255, 255, 0.9); /* Slightly more opaque on hover */
  backdrop-filter: blur(100px); /* Apply blur effect */
  opacity: 0;
  transition: opacity 0.3s, transform 0.3s, background-color 0.3s;
  z-index: 3; /* Ensure text is above both the image and overlay */
}

.individual:hover p {
  opacity: 1;
  transform: scale(1.2); /* Increase the size of the button */
  background-color: rgba(255, 255, 255, 0.1); /* Slightly more opaque on hover */
}

/* Mobile styling */
@media (max-width: 600px) {
  .gallery {
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  }

  .individual p {
    font-size: 12px;
  }
}

/* Two-column layout for widths up to 910px */
@media (max-width: 910px) {
  .gallery {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Auto-resizing thumbnail view for widths greater than 911px */
@media (min-width: 911px) {
  .gallery {
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  }

  .individual p {
    font-size: 14px;
  }
}

.footer {
  clear: both;
  position: relative;
  z-index: 10;
  padding: 20px;
}

.container::after {
  content: "";
  display: block;
  height: 500px; /* Adjust this height to add more space at the bottom */
  clear: both;
}

.footer-content {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

.footer-section {
  flex: 1;
  min-width: 200px;
  margin: 10px;
}

.footer-bottom {
  text-align: center;
  padding: 10px;
  border-top: 1px solid var(--secondary-color);
}

.back-to-top {
  display: inline-block;
  margin-top: 10px;
  color: var(--accent-color);
  text-decoration: none;
}

.back-to-top:hover {
  text-decoration: underline;
}
